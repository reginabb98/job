name: Refresh NYC events site

on:
  schedule:
    # 13:00 UTC = 9am ET (8am during EST) — once a day is plenty, venue
    # listings don't change hour to hour.
    - cron: "0 13 * * *"
  workflow_dispatch: {}  # lets you click "Run workflow" in the Actions tab for an on-demand refresh

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: nyc-events
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Scrape venues and build the taste-matched feed
        env:
          LASTFM_API_KEY: ${{ secrets.LASTFM_API_KEY }}
          TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
        run: python run.py

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: nyc-events

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
